{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -q 'git commit' && git push 2>&1 || true",
            "statusMessage": "Pushing to remote..."
          }
        ]
      }
    ]
  }
}
